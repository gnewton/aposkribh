

%%Title:
%%Creator:Glen Newtopn
%%BoundingBox:0 0 400 400
%%%%%%%%%%%%%% 
%<< /AllowTransparency true >> % enable transparency


/w 400 def
/h 600 def
/dx 300 def
/dy 300 def


dx  dy  translate
.5 .5 scale

/axis{
    gsave
    1 setlinewidth
    newpath
    0 0 moveto
    w  0 lineto
    0 0 moveto
    0 h lineto
    stroke
    grestore
}def

/dot{
    1 dict begin
    /size exch def
    gsave
    translate
    0 0 size 0 360 arc closepath fill
    grestore
    end
}bind def

/Pi 3.14159 def
/Pi2 Pi 2.0 mul def
/PiE2 Pi Pi mul def

/E 2.71828 def

/variance .05 def
/variance2 variance 2 mul def
/standardDeviation variance sqrt def
% https://pages.stat.wisc.edu/~mchung/teaching/MIA/reading/diffusion.gaussian.kernel.pdf.pdf p4


/gauss{ % returns 0 to 1 real (prob x)
    2 dict begin
    /x exch def
    /ev E x x mul variance2 div neg exp def
    ev
    end
}bind def

/w2 w 2 div def

/MaxInt 2147483647 def
/MaxIntAsReal MaxInt cvr def

/H 10.0 def
/H2 H 2.0 div def

1000000 {
    /x rand cvr MaxIntAsReal div H mul H2 sub def
    /y rand cvr MaxIntAsReal div def

    /py x gauss def

    py rand cvr MaxIntAsReal div gt{
	1 0 0 setrgbcolor
	x w mul H2 sub  y h mul 1 dot
    }if
}repeat

w neg 5 mul 1 w 5 mul{
    /x exch def
    gsave
    0 0 1 setrgbcolor
    /y x cvr w div gauss def
    x H2 sub  y h mul   2 dot
    grestore
}for

0 setgray
axis

/foo 115910200641878280303 def
foo =
foo type =

foo 1 add =
foo 100 div cvi =
(MMMMMM) =


/bar 9223372036854775807 def

bar =
bar neg =
bar 1 sub type =
bar 1 add type =

showpage
